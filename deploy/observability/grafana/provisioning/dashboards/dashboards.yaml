# Grafana Dashboard Provisioning
apiVersion: 1

providers:
  - name: 'VEDS Dashboards'
    orgId: 1
    folder: 'VEDS'
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
